'use strict';

angular.module('myApp.contact', ['ngRoute'])
    .controller('ContactCtrl', ['$scope','$rootScope',function($scope,$rootScope) {
       // $rootScope.bodylayout = 'output no-fluid reader-day-mode reader-font2 ';
    }]);